import { sharedOptions, defaultPackageRules } from './shared/shared-settings'

const options = {
  repositories: [ 'cenk1cenk2/boilerplate-oclif' ],
  ...sharedOptions,
  ...defaultPackageRules
}

export default options
