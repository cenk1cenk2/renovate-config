amends "package://github.com/jdx/hk/releases/download/v2.0.1/hk@2.0.1#/Config.pkl"
import "package://github.com/jdx/hk/releases/download/v2.0.1/hk@2.0.1#/Builtins.pkl"

hooks {
  ["pre-commit"] {
    fix = true
    stash = "git"
    steps {
      ["prettier"] = (Builtins.prettier) {
        glob = List("*.ts", "*.js", "*.tsx", "*.jsx", "*.json", "*.md")
      }
      // `pnpm lint` and the CI lint job both run eslint over `./src` only:
      // the root config files are eslint-ignored and `test/` is outside the
      // tsconfig project the typed config parses against.
      ["eslint"] = (Builtins.eslint) {
        glob = List("src/**/*.ts")
        depends = List("prettier")
      }
    }
  }
}
